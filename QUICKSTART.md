# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Настройка

Отредактируй файл `.env`:

```bash
BITRIX_WEBHOOK=https://your-portal.bitrix24.ru/rest/USER_ID/CODE/
```

## Шаг 3: Создание примера архива (опционально)

```bash
python create_example.py
```

Это создаст `example_archive.zip` с тестовыми данными.

## Шаг 4: Запуск бота

### Вариант А: Локально

```bash
python bitrix_bot.py
```

Бот запустится на `http://localhost:5000`

### Вариант Б: Docker

```bash
docker-compose up -d
```

## Шаг 5: Публичный доступ (для Bitrix24)

Используй ngrok для получения публичного URL:

```bash
# Установи ngrok
brew install ngrok  # macOS
# или скачай с https://ngrok.com

# Запусти туннель
ngrok http 5000
```

Используй полученный URL (например, `https://abc123.ngrok.io`) для настройки вебхука в Bitrix24.

## Шаг 6: Тестирование

Проверь работоспособность:

```bash
curl http://localhost:5000/health
```

Должен вернуть: `{"status": "ok", "timestamp": "..."}`

## 📦 Формат архива

Создай ZIP архив:

```
archive.zip
├── data.xlsx           # Excel с данными (обязательно)
└── img/                # Картинки (опционально)
    ├── logos/          # company.png
    ├── certificates/   # еас.png, рст.png
    └── mark_images/    # mark_images.png
```

## 🤖 Использование в Bitrix24

1. Открой чат с ботом в Bitrix24
2. Напиши `/start`
3. Отправь ZIP архив
4. Получи результат (labels + marks в PDF)

## 🐛 Проблемы?

Смотри логи:

```bash
# Локально
python bitrix_bot.py

# Docker
docker-compose logs -f
```

## 📚 Подробнее

Смотри [README.md](README.md) для полной документации.